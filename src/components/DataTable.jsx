import React from 'react';
import { TableVirtuoso } from 'react-virtuoso';

const DataTable = ({ data }) => {
  return null; // Таблица скрыта, так как требуется только отображение графиков
};

export default DataTable;